# for ounit testing
true: package(oUnit)

# for bisect coverage checking
<sorts.ml>: coverage
<test_sorts.{byte,native}>: coverage
true: package(ounit2), package(bisect_ppx)
true: package(oUnit), package(graphics)
